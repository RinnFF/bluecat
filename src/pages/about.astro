---
import BaseLayout from '../layouts/BaseLayout.astro';
const pageTitle = "About the cat";

const identity = {
  firstName: "Mister stripes",
  country: "Planet Earth",
  occupation: "Wise companion",
  hobbies: ["lounging", "birdwatching", "snacking"],
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Writing Docs"];

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "navy";
const fontWeight = "bold";
const textCase = "uppercase";
---
<BaseLayout pageTitle={pageTitle}>
	<h1>Some info:</h1>
		{happy && <p>I am introduce the real me.</p>}
        <ul>
  <li>My name is {identity.firstName}.</li>
  <li>I live on {identity.country} and I consider myself a {identity.occupation}.</li>
  {identity.hobbies.length >= 2 &&
    <li>My main hobbies are: {identity.hobbies[0]}, {identity.hobbies[1]} and {identity.hobbies[2]}</li>
  }
</ul>
<p>Since I am a cat, my skills definitely do not include:</p>
<ul>
  {skills.map((skill) => <li class="skill">{skill}</li>)}
</ul>
	{happy && <p>My friend is happy to be learning Astro!</p>}
	{goal === 3 ? <p>My goal is to make some progress.</p> : <p>My goal is to have a nap.</p>}
	<p>Update:</p>
	{finished && <p>She finished this tutorial!</p>}
	<style>
      h1 {
        color: purple;
        font-size: 4rem;
      }
	  .skill {
    	color: var(--skillColor);
    	font-weight: var(--fontWeight);
		text-transform: var(--textCase);
  	  }
    </style>
</BaseLayout>
    
